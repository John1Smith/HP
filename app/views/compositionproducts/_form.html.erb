<%= form_for(@compositionproduct) do |f| %>
  <% if @compositionproduct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compositionproduct.errors.count, "error") %> prohibited this compositionproduct from being saved:</h2>

      <ul>
      <% @compositionproduct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :product_id, Product.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="field">
   <%= f.select :composition_id, Composition.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
